<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.msBackend.mapper.AdminAuthMapper">
    
    <!-- 根据用户名查询管理员 -->
    <select id="findByUsername" resultType="com.example.msBackend.pojo.Admin">
        SELECT id, username, password, nickname, email, avatar, status
        FROM admin
        WHERE username = #{username}
        LIMIT 1
    </select>
    
    <!-- 根据ID查询管理员 -->
    <select id="findById" resultType="com.example.msBackend.pojo.Admin">
        SELECT id, username, password, nickname, email, avatar, status
        FROM admin
        WHERE id = #{id}
        LIMIT 1
    </select>
    
</mapper>
